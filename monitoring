-- ================= CONFIGURATION =================
TARGET_TYPES = "all"            -- "all" untuk trade semua jenis elixir, atau tabel misal {1, 2}
BLACKLIST_TYPES = {}            -- Tipe yang dikecualikan (kosongkan jika tidak ada)
TARGET_TRADE_PLAYER = "mainuhuy"-- Nama player target utama[cite: 1]
-- =================================================

if not game:IsLoaded() then 
    repeat task.wait(1) until game:IsLoaded() 
end

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

print("[AutoTrade] Auto-Execute terdeteksi. Menunggu karakter spawn...")

-- WAJIB: Tunggu sampai karakter player benar-benar muncul di dalam dunia game
local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
LocalPlayer:WaitForChild("PlayerGui")

print("[AutoTrade] Karakter siap! Mulai memuat modul game...")

-- 1. LOAD CLIENT PILL MANAGER DENGAN AMAN
local clientPillManager = nil
local successLoad, err = pcall(function()
    local pillModule = ReplicatedStorage:WaitForChild("脚本模块", 15)
        :WaitForChild("客户端", 15)
        :WaitForChild("工具", 15)
        :WaitForChild("客户端丹药管理器", 15)
    return require(pillModule)
end)

if successLoad and successLoad then
    clientPillManager = err
    print("[AutoTrade] Sukses! Client Pill Manager berhasil dimuat.")
else
    warn("[AutoTrade] Gagal memuat Pill Manager via require. Error: " .. tostring(err))
    return
end

-- 2. PATH REMOTE EVENTS
local eventFolder = ReplicatedStorage:WaitForChild("脚本", 15) -- Fallback jika folder utama berbeda, atau tetap pakai string cina:
pcall(function()
    eventFolder = ReplicatedStorage:WaitForChild("\228\186\139\228\187\182", 5)
        :WaitForChild("\229\133\172\231\148\168", 5)
        :WaitForChild("\228\186\164\230\152\147", 5)
end)

local tradeRemote = eventFolder:WaitForChild("\230\150\176\229\162\158\228\186\164\230\152\147\231\137\169\229\147\129", 10)
local requestTradeRemote = eventFolder:WaitForChild("\231\148\179\232\175\183\228\186\164\230\152\147", 10)
local confirmTradeRemote = eventFolder:WaitForChild("\233\148\129\229\174\154\228\186\164\230\152\147", 10)

print("[AutoTrade] Remote Events berhasil di-link!")

-- 3. FUNGSI INVENTORI LIVE
local function GetLiveInventory()
    local successUp, uvs = pcall(function()
        return debug.getupvalues(clientPillManager["计算可用丹药积分"])
    end)
    if successUp and uvs and uvs[1] and uvs[1]["背包"] then
        return uvs[1]["背包"]
    end
    return nil
end

local function tableContains(tbl, val)
    for _, v in ipairs(tbl) do
        if v == val then return true end
    end
    return false
end

local function isAllowedToTrade(val)
    if tableContains(BLACKLIST_TYPES, val) then return false end
    if type(TARGET_TYPES) == "string" then
        local lowerTarget = string.lower(TARGET_TYPES)
        if lowerTarget == "all" or lowerTarget == "1-5" then
            return val >= 1 and val <= 5
        end
    elseif type(TARGET_TYPES) == "table" then
        return tableContains(TARGET_TYPES, val)
    end
    return false
end

local function hasElixirInInventory()
    local inventory = GetLiveInventory()
    if not inventory then return false end
    
    for _, item in ipairs(inventory) do
        local uuid = item["索引"]
        local typeVal = item["类型"] or 0
        if uuid and isAllowedToTrade(typeVal) then
            return true
        end
    end
    return false
end

local function ProcessAllItems()
    local inventory = GetLiveInventory()
    if not inventory then return false end

    print("[AutoTrade] Memasukkan semua elixir ke trade...")
    local successCount = 0

    for i, item in ipairs(inventory) do
        local uuid = item["索引"]
        local typeVal = item["类型"] or 0
        
        if uuid and isAllowedToTrade(typeVal) then
            local args = {
                [1] = "\228\184\185\232\141\175",
                [2] = uuid,
                [3] = 99999
            }
            
            pcall(function()
                tradeRemote:FireServer(unpack(args))
            end)
            
            successCount = successCount + 1
            task.wait(0.1)
        end
    end

    print(string.format("[AutoTrade] Selesai! Total dimasukkan: %d item", successCount))
    return successCount > 0
end

-- 4. EKSEKUSI UTAMA
task.spawn(function()
    task.wait(3) -- Jeda ekstra setelah karakter berdiri stabil
    
    while true do
        local itemAvailable = hasElixirInInventory()
        
        if itemAvailable then
            local targetPlayer = Players:FindFirstChild(TARGET_TRADE_PLAYER)
            
            if targetPlayer then
                local argsReq = { [1] = targetPlayer }
                pcall(function()
                    requestTradeRemote:FireServer(unpack(argsReq))
                    print("[AutoTrade] Mengirim request trade ke " .. TARGET_TRADE_PLAYER .. "...")[cite: 1]
                end)
                
                task.wait(2)
                local sent = ProcessAllItems()
                
                if sent then
                    task.wait(1)
                    pcall(function()
                        local argsConfirm = { [1] = true }
                        confirmTradeRemote:FireServer(unpack(argsConfirm))
                        print("[AutoTrade] Semua item masuk, bot menekan tombol setuju (true)!")
                    end)
                end
            else
                warn("[AutoTrade] Player '" .. TARGET_TRADE_PLAYER .. "' tidak ditemukan di server!")[cite: 1]
            end
        else
            print("[AutoTrade] Elixir di tas habis atau belum terbaca. Menunggu...")
        end
        
        task.wait(6)
    end
end)
